name: Deploy Frontend

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Deployed via Vercel
        run: echo "Vercel auto-deploys on push to main. No action needed."
